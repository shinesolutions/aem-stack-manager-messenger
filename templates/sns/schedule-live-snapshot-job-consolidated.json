{
  "externalId": "{{ externalId }}",
  "task": "schedule-snapshot",
  "stack_prefix": "{{ target_aem_stack_prefix }}",
  "details": {
    "component": ["author-publish-dispatcher"],
    "comment": "Set scheduled live-snapshot job on AEM Consolidated architecture",
    "parameters": {
      "jobName": ["{{ schedule_job }}"],
      "State": ["enable"]
    }
  }
}
