{
  "task": "import-package",
  "stack_prefix": stack_prefix,
  "details": {
    "component": [component],
    "comment": "import AEM backed up pacakges for a stack based on group, name and timestamp",
    "parameters": {
      "sourceStackPrefix": [source_stack_prefix],
      "packageGroup": [package_group],
      "packageName": [package_name],
      "packageDatestamp": [package_datestamp],
      "aemId": [aem_id]
    }

  }
}
