{
    "task": "run-adhoc-puppet",
  "stack_prefix": "{{ target_aem_stack_prefix }}",
  "details": {
    "component": ["{{ component }}"],
    "comment": "Run adhoc puppet code on selected instances",
    "parameters": {
      "adhocPuppetFile": ["{{ puppet_tar_file }}"]
    }
  }
}
